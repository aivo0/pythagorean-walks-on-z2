[package]
name = "pythagorean_walks_fast"
version = "0.1.0"
edition = "2021"

[lib]
name = "pythagorean_walks_fast"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.22.6", features = ["extension-module"] }
